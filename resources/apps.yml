resources:
  apps:
    medical_logistics_nba_app:
      name: "${bundle.target}-med-logistics-nba"
      description: "Next Best Action agent for medical logistics operations"
      source_code_path: ../app

      resources:
        - name: warehouse_id
          sql_warehouse:
            id: ${var.warehouse_id}
            permission: "CAN_USE"
